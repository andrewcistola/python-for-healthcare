#### Prep Code

library(tidyverse)

setwd("E:/DataStories")

oecd = read.csv("_data/wealth_oecd_stage.csv")
head(oecd)

health = read.csv("_data/wealth_health_stage.csv")
head(health)

R:Create R terminal
